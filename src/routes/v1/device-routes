const validateCreateDevice = require('../../middlewares/validateCreateDevice');
const express = require('express');
const router = express.Router();
const deviceController = require('../../controllers/device-controller');

router.post('/admin/devices', validateCreateDevice, deviceController.createDevice);

module.exports = router;

